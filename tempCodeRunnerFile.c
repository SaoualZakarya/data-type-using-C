int n, i, *p = NULL;
    // printf("Enter the number of elements: ");
    // scanf("%d", &n);
    // p = calloc(n, sizeof(p)); // we can use: p = malloc(sizeof(p) * n);  
    // for (i = 0; i < n; i++)
    // {
    //     printf("Enter element %d: \n", i + 1); 
    //     scanf("%d", p + i); 
    // }

    // printf("The elements are: "); 

    // for (i = 0; i < n; i++)
    // {
    //     printf("%d \n", *(p + i));
    // }
    // free(p);
    